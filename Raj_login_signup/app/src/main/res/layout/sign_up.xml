<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <EditText
        android:id="@+id/su_email"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Enter Email"
        android:inputType="textEmailAddress"
        android:textAlignment="center"
        android:layout_marginBottom="20dp"
        android:layout_above="@+id/su_username"
        android:layout_centerHorizontal="true" />

    <EditText
        android:id="@+id/su_username"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Enter Username"
        android:inputType="textPersonName"
        android:textAlignment="center"
        android:layout_marginBottom="57dp"
        android:layout_alignBottom="@+id/su_password"
        android:layout_alignLeft="@+id/su_email"
        android:layout_alignStart="@+id/su_email" />

    <EditText
        android:id="@+id/su_password"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Enter Password"
        android:inputType="textPassword"
        android:textAlignment="center"
        android:layout_marginBottom="13dp"
        android:layout_above="@+id/su_re_password"
        android:layout_alignLeft="@+id/su_username"
        android:layout_alignStart="@+id/su_username" />

    <EditText
        android:id="@+id/su_re_password"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Confirm Password"
        android:inputType="textPassword"
        android:textAlignment="center"
        android:layout_marginBottom="105dp"
        android:layout_above="@+id/su_sign_up"
        android:layout_alignLeft="@+id/su_password"
        android:layout_alignStart="@+id/su_password" />

    <Button
        android:id="@+id/su_sign_up"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sign up"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="40dp" />



</RelativeLayout>